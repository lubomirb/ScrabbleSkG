name: Deploy ScrabbleSkG to GitHub Pages

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x

    - name: Publish Blazor WASM
      run: dotnet publish ScrabbleSkX/ScrabbleSkX.csproj -c Release -o publish

    - name: Add .nojekyll
      run: touch publish/wwwroot/.nojekyll

    - name: Rewrite base href
      uses: stevesandersonms/ghaction-rewrite-base-href@5b54862
      with:
        html_path: publish/wwwroot/index.html
        base_href: /ScrabbleSkG/

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: publish/wwwroot

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
